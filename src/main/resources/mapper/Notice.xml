<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.itupo.iiv.dao.NoticeDao">
	<insert id="writePost" parameterType="io.itupo.iiv.domain.NoticeBean">
		INSERT INTO notice (sector, title, content, user_id, write_date, views) 
		VALUE (#{sector}, #{title}, #{content}, 0, #{userId}, NOW(), 0)
	</insert>
</mapper>
